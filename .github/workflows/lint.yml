name: Python Indentation Check

on: [push, pull_request]

jobs:
  check-indentation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Ruff
        run: pip install ruff

      - name: Check Formatting & Indentation
        run: ruff format --check .
